const find_odd_or_even = (num) => {
    if (num % 2 == 0) {
        console.log(`Number is ${num} : Even`);
    } else {
        console.log(`Number is ${num} : Odd`);
    }
};

let num = 7;
find_odd_or_even(num);
